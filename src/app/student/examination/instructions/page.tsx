'use client'

import Link from 'next/link'
import { useEffect, useState } from 'react'
import { useRouter } from 'next/navigation'
import { getExamData, type Exam } from '@/lib/exam'

export default function InstructionsPage() { const router = useRouter(); const [exam, setExam] = useState<Exam | null>(null); const [loading, setLoading] = useState(true); useEffect(() => { getExamData('default').then(({ exam: value }) => setExam(value)).catch(() => setExam(null)).finally(() => setLoading(false)) }, []); if (loading) return <main className="mx-auto max-w-3xl px-6 py-12 text-sm text-slate-500">Loading instructions…</main>; if (!exam) return <main className="mx-auto max-w-3xl px-6 py-12"><div className="rounded-2xl border border-[#d8e4ed] bg-white p-8 text-[#123b5d]">Examination is currently unavailable.</div></main>; return <main className="mx-auto max-w-3xl px-6 py-12"><section className="rounded-3xl border border-[#d8e4ed] bg-white p-8 shadow-xl shadow-[#123b5d]/5"><p className="text-xs font-bold uppercase tracking-[0.22em] text-[#287a78]">Before you begin</p><h1 className="mt-2 text-3xl font-bold text-[#123b5d]">Examination Instructions</h1><p className="mt-2 text-slate-500">{exam.duration_minutes} minutes · {exam.total_marks} marks</p><ul className="mt-8 flex flex-col gap-4 text-sm leading-6 text-slate-600"><li>Keep a stable internet connection throughout the examination.</li><li>Your timer continues after refresh and the attempt auto-submits when time expires.</li><li>Use the palette to navigate and mark questions for review.</li><li>Once submitted, answers cannot be changed.</li></ul><div className="mt-8 flex gap-3"><Link href="/student/examination/verification" className="rounded-xl border border-slate-300 px-5 py-3 text-sm font-bold text-slate-700">Back</Link><button onClick={() => router.push(`/exam/${exam.id}/attempt`)} className="rounded-xl bg-[#287a78] px-5 py-3 text-sm font-bold text-white hover:bg-[#216462]">Start Examination</button></div></section></main> }
