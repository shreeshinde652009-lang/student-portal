'use client'

import Link from 'next/link'
import { useSearchParams } from 'next/navigation'

export default function ResultPage() { const params = useSearchParams(); const score = Number(params.get('score') || 0); const total = Number(params.get('total') || 0); const status = params.get('status') || 'submitted'; const passed = total > 0 && score >= Math.ceil(total * 0.4); return <main className="mx-auto max-w-2xl py-8"><section className="rounded-xl border border-slate-200 bg-white p-8 text-center shadow-sm"><div className={`mx-auto flex size-16 items-center justify-center rounded-full text-2xl font-bold ${passed ? 'bg-emerald-100 text-emerald-800' : 'bg-amber-100 text-amber-800'}`}>{passed ? '✓' : '!'}</div><p className="mt-5 text-xs font-semibold uppercase tracking-[0.18em] text-blue-700">Examination submitted</p><h1 className="mt-2 text-3xl font-bold text-slate-900">Your result is recorded</h1><p className="mt-2 text-sm text-slate-500">Submission status: {status}</p><div className="mx-auto mt-7 max-w-xs rounded-xl bg-slate-50 p-6"><p className="text-sm text-slate-500">Score</p><p className="mt-1 text-5xl font-bold text-slate-900">{score}<span className="text-2xl text-slate-400">/{total}</span></p><p className={`mt-2 text-sm font-semibold ${passed ? 'text-emerald-700' : 'text-amber-700'}`}>{passed ? 'Eligible for the next stage' : 'Result recorded for review'}</p></div><Link href="/exam/dashboard" className="mt-7 inline-block rounded-lg bg-blue-800 px-5 py-3 text-sm font-semibold text-white">Return to dashboard</Link></section></main> }
